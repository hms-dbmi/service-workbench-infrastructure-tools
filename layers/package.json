{
  "name": "swb-tools-layers",
  "description": "Generate and upload common js dependency packages for use in lambdas",
  "scripts": {
    "sls": "serverless",
    "build-layers": "npm-recursive-install --skip-root --omit=dev"
  },
  "devDependencies": {
    "recursive-install": "^1.4.0",
    "serverless": "^3.22.0",
    "serverless-deployment-bucket": "^1.6.0",
    "serverless-scriptable-plugin": "^1.2.2"
  }
}
