{
  "name": "swb-tools-registration-api",
  "description": "Inserts a new user into the user database, ready for IDP flow.",
  "scripts": {
    "test": "jest",
    "sls": "serverless"
  },
  "devDependencies": {
    "aws-sdk": "^2.1176.0",
    "jest": "^28.1.3",
    "serverless": "^3.22.0",
    "serverless-iam-roles-per-function": "^3.2.0",
    "uuid": "^8.3.2"
  }
}